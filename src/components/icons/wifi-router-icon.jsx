import { Iconify } from 'src/components/iconify';

// ----------------------------------------------------------------------

export function WifiRouterIcon({ sx, ...other }) {
  return <Iconify icon="solar:wifi-router-bold" sx={sx} {...other} />;
}
