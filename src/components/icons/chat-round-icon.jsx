import { Iconify } from 'src/components/iconify';

// ----------------------------------------------------------------------

export function ChatRoundIcon({ sx, ...other }) {
  return <Iconify icon="solar:chat-round-dots-bold" sx={sx} {...other} />;
}
