import { Iconify } from 'src/components/iconify';

// ----------------------------------------------------------------------

export function UserCheckIcon({ sx, ...other }) {
  return <Iconify icon="solar:user-check-bold" sx={sx} {...other} />;
}
