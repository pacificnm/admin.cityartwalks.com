import { Iconify } from 'src/components/iconify';

// ----------------------------------------------------------------------

export function BrainIcon({ sx, ...other }) {
  return <Iconify icon="solar:brain-bold" sx={sx} {...other} />;
}
